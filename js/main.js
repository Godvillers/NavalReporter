// Generated by CoffeeScript 1.12.2
var body, collectData, doc, every, form, getHTML, getLastSegment, getTurn, host, id, sendData, timer, winName;

host = "http://localhost:8000";

winName = "naval-reporter-win";

doc = document;

body = doc.body;

id = form = null;

every = function(ms, action) {
  return setInterval(action, ms);
};

getLastSegment = function(url) {
  return url.match(/\/[^\/]*$/)[0].slice(1);
};

getTurn = function() {
  try {
    return +doc.querySelector("#m_fight_log .block_h .block_title").innerText.match(/\d+/)[0];
  } catch (error) {
    return 0;
  }
};

getHTML = function(id) {
  try {
    return doc.getElementById(id).outerHTML;
  } catch (error) {
    return "";
  }
};

collectData = function() {
  return {
    id: id,
    turn: getTurn(),
    allies: getHTML("alls"),
    map: getHTML("s_map"),
    log: getHTML("m_fight_log")
  };
};

sendData = function(data) {
  var i, input, len, ref;
  ref = form.children;
  for (i = 0, len = ref.length; i < len; i++) {
    input = ref[i];
    input.value = data[input.name];
  }
  open("about:blank", winName, "toolbar=no,scrollbars=no,location=no,status=no,menubar=no,resizable=no,height=90,width=400");
  body.appendChild(form);
  form.submit();
  return body.removeChild(form);
};

timer = every(300, function() {
  var heroBlock, name, streamingLink;
  if (doc.getElementById("hero_columns") == null) {
    return;
  }
  clearInterval(timer);
  if (doc.getElementById("s_map") == null) {
    return;
  }
  id = getLastSegment(doc.getElementById("fbclink").href);
  form = doc.createElement("form");
  form.method = "POST";
  form.action = host + "/send";
  form.enctype = "multipart/form-data";
  form.acceptCharset = "utf-8";
  form.target = winName;
  form.innerHTML = ((function() {
    var i, len, ref, results;
    ref = ["id", "turn", "allies", "map", "log"];
    results = [];
    for (i = 0, len = ref.length; i < len; i++) {
      name = ref[i];
      results.push("<input type='hidden' name='" + name + "' />");
    }
    return results;
  })()).join("");
  heroBlock = doc.getElementById("hero_block");
  heroBlock.insertAdjacentHTML("afterbegin", '<div style="text-align: center;"><a href="#" target="_blank">Транслировать</a></div>');
  streamingLink = heroBlock.firstChild.firstChild;
  return streamingLink.onclick = function() {
    var data, lastTurn;
    streamingLink.text = "Идёт трансляция";
    streamingLink.href = host + "/duels/log/" + id;
    streamingLink.onclick = null;
    data = collectData();
    lastTurn = data.turn;
    sendData(data);
    every(500, function() {
      var turn;
      turn = getTurn();
      if (turn <= lastTurn) {
        return;
      }
      lastTurn = turn;
      return sendData(collectData());
    });
    return false;
  };
});
